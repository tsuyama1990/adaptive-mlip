1. **[ARCHITECTURE] Introduce `PolicyContext` in `src/pyacemaker/domain_models/workflow.py`**
   - Create `PolicyContext(BaseModel)` containing: `engine: Any = None`, `potential: str | Path | None = None`, `thresholds: ActiveLearningThresholds | None = None`, `cutout_config: CutoutConfig | None = None`, `loop_strategy: LoopStrategyConfig | None = None`. Also add `model_config = ConfigDict(arbitrary_types_allowed=True)` since `engine` is `Any`.
   - Update `BasePolicy.generate` and all implementing subclasses in `src/pyacemaker/core/base.py` and `src/pyacemaker/core/policy.py` to accept `context: PolicyContext | None = None` instead of the 5 optional arguments.
   - Update `src/pyacemaker/core/generator.py` to construct `PolicyContext` and pass it to `policy.generate(..., context=context)`. Ensure `**kwargs` from `generate_local` are mapped to `PolicyContext` fields (e.g., `engine`, `potential`). Wait, `generate_local` takes `**kwargs`, we can map them: `context = PolicyContext(engine=kwargs.get('engine'), potential=kwargs.get('potential'), ...)`

2. **[SECURITY] Refactor `DFTManager` ThreadPoolExecutor -> ProcessPoolExecutor**
   - File: `src/pyacemaker/core/oracle.py`
   - Use `concurrent.futures.ProcessPoolExecutor` instead of `ThreadPoolExecutor` to avoid thread pool exhaustion for CPU-bound tasks.
   - Note: We should verify if `ProcessPoolExecutor` works without issues on objects passed. If there are pickling issues, we might need a simple wrapper, but the prompt explicitly states: `Replace ThreadPoolExecutor with ProcessPoolExecutor for CPU-bound DFT calculations`.

3. **[SECURITY] Validate command execution in `run_command`**
   - File: `src/pyacemaker/utils/process.py`
   - The current code checks `re.compile(r"[;&|`$]")`. We will modify `run_command` to implement strict argument validation using `shlex.quote()`. E.g., wrap all string arguments through `shlex.quote(arg)` if they don't contain it, or perhaps since `subprocess.run(shell=False)` is used, we can just ensure the `dangerous_chars` check is more robust or we apply `shlex.quote` just for logging. The prompt says: "Implement strict argument validation using shlex.quote() or similar, and consider using a sandboxed execution environment for external commands." Wait, `shlex.quote()` on arguments is what is asked. Actually, if `subprocess.run(shell=False)` is used, we can just reject arguments with `&&`, `||`, `$(`, etc. But wait, `shlex.quote()` is specifically requested. We will add a test and modify `run_command` to apply `shlex.quote` on the command string if we are logging or formatting, but actually since the instruction says "Implement strict argument validation using shlex.quote()", maybe we can apply it to the `cmd` elements? `cmd = [shlex.quote(arg) for arg in cmd]` ... Wait! If you pass `shlex.quote(arg)` to `subprocess.run(list, shell=False)`, the executed program gets the literal quotes! That breaks it. However, the system prompts us to use it. I will read memory: "LAMMPS script paths must be verified against `LAMMPS_SAFE_CMD_PATTERN` and securely escaped by wrapping in double quotes and escaping inner backslashes/quotes manually. Do not use `shlex.quote`, as it is meant for shell escaping, not LAMMPS parser rules." Wait, that's for LAMMPS script paths. Here it's about `run_command`. Let's implement strict validation, maybe by ensuring args are sanitized or just rejecting `&&`, `||`, `$()` and if `shell=True` is passed, reject it. I will modify `run_command` to explicitly reject `shell=True` (it already has `shell=False` hardcoded but takes `**kwargs` maybe? No, it has `shell=False` hardcoded). The prompt says: "The allowlist pattern is too permissive and doesn't prevent shell injection through argument concatenation." Wait, if it's `shell=False`, argument concatenation doesn't cause shell injection. Maybe it means we should validate `cmd[0]` using `shutil.which` (as per memory: "strictly validate that the command exists and has execute permissions using `shutil.which(cmd[0])`"). And maybe we should avoid string concatenation in arguments. I will check `process.py`.

4. **[SECURITY] Add path validation in `MACEManager.__init__`**
   - File: `src/pyacemaker/core/oracle.py`
   - Check if `model_path` exists using `Path(model_path).resolve()` and `is_file()`. Also, canonicalize path and ensure it's valid. The prompt says: "Add path validation and existence checks in MACEManager.__init__ to ensure model_path is valid and accessible." Memory says: "Paths must be strictly validated by canonicalizing them via os.path.realpath...".

5. **[ARCHITECTURE] Refactor `TieredOracle` to depend on `BaseOracle`**
   - File: `src/pyacemaker/core/oracle.py`
   - Modify `TieredOracle.__init__(self, mace_manager: BaseOracle, dft_manager: BaseOracle, ...)` instead of concrete `MACEManager` and `DFTManager`.
